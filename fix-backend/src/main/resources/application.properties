spring.application.name=feedback

# Port Number
server.port=8081

# MySQL Configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/project?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Varsha123#

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# SQL Init - run schema.sql on startup to ensure all tables exist (IF NOT EXISTS guards prevent duplicates)
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.jpa.defer-datasource-initialization=true

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=varshinijayavarapu3@gmail.com
spring.mail.password=abtc qtvz acdk viux
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# JWT Configuration
app.jwt.secret=EduFeedbackSecretKey2024SuperSecureBase64EncodedKey12345678
app.jwt.expiration=86400000

# Google reCAPTCHA v2
# Get your keys from: https://www.google.com/recaptcha/admin
recaptcha.secret.key=your_recaptcha_secret_key_here
recaptcha.verify.url=https://www.google.com/recaptcha/api/siteverify

# CORS - frontend runs on 5173
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# OTP Settings
app.otp.expiry-minutes=10
